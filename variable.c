#include <stdio.h>

int main () {

   /* variable declaration: */
   int age;
   char x;

   /* variable definition */
   age = 10;
   x = 'x';

   /* variable declaration and definition */
   char address[] = "hà nội";

   printf("value of age : %d \n", age);
   printf("value of age : %c \n", x);
   printf("value of address : %s \n", address);

   return 0;
}